{
  "name": "investpro",
  "version": "2.0.0",
  "description": "InvestPro — Digital Investment Platform",
  "private": true,
  "scripts": {
    "start":   "cd backend && npm start",
    "dev":     "cd backend && npm run dev",
    "seed":    "cd backend && npm run seed",
    "install": "cd backend && npm install",
    "package:frontend": "powershell -ExecutionPolicy Bypass -File scripts/package-frontend.ps1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
